# NixOS build results
result
result-*

# Secrets. These live in /var/lib/<service>/ on the host and are generated by
# `just install`; nothing here should ever contain one, but belt and braces.
*.env
!.env.example
*-pass
secrets/

# Editor / OS noise
*.log
*.tmp
*.swp
*~
