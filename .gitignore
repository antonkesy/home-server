# NixOS build results
result
result-*

# secrets live in /var/lib/<service>/ on the host (gen-secrets); belt and braces
*.env
admin-pass
secrets/

# `just backup .` would drop an archive here
*.tar.zst

# Editor / OS noise
*.log
*.tmp
*.swp
*~
